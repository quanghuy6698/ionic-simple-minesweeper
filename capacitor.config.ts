import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ironhuy.simpleMinesweeper',
  appName: 'Simple Minesweeper',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
